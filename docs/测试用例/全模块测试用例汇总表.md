# 全模块测试用例汇总表

> **版本**：V2
> **编制日期**：2026-07-16
> **编制人**：测试工程师（5号）-张宇
> **用途**：一张表看清所有模块的测试用例覆盖情况和当前可执行状态

---

## 状态说明

| 标记 | 含义 |
|:--:|------|
| ✅ 可测 | 当前可以执行该用例 |
| 🔴 阻塞 | 依赖后端/前端修复 Bug 后才能执行 |
| ⏳ 待开发 | 对应功能模块尚未开发完成 |
| ✔️ 已测 | 已经执行并通过 |

---

## 一、鉴权模块（TC-A）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-A01 | 正常登录 | 输入正确用户名+密码+角色，点击登录 | 登录成功，跳转到对应首页，localStorage 存储 token 和 userInfo | ✅ 可测 | — |
| TC-A02 | 错误密码登录 | 输入正确用户名+错误密码，点击登录 | 登录失败，提示错误，不跳转 | ✅ 可测 | — |
| TC-A03 | 未登录访问受保护页面 | 清除 localStorage，直接访问 `/dashboard` | 重定向到 `/login` | ✅ 可测 | — |
| TC-A04 | Token 失效后访问 | 手动修改 localStorage 中 token 为无效值，刷新页面访问 API | 返回 401，前端拦截跳转登录页 | 🔴 阻塞 | 后端登录接口未实现，无法模拟真实 Token 失效场景 |

---

## 二、用户管理模块（TC-U）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-U01 | 查看用户列表 | 管理员登录后进入用户管理页 | 展示用户列表，包含 ID/用户名/显示名/角色/状态/创建时间 | ✅ 可测 | — |
| TC-U02 | 新增用户 | 管理员点击"新增用户"，填写用户名/密码/显示名/角色/状态，提交 | 列表刷新，新增用户出现 | ✅ 可测 | — |
| TC-U03 | 编辑用户信息 | 管理员点击用户行的"编辑"，修改显示名或角色，提交 | 列表刷新，用户信息更新 | ✅ 可测 | — |
| TC-U04 | 删除（停用）用户 | 管理员点击用户行的"删除"，确认弹窗中确认 | 用户状态变为"已停用"，标签变红 | ✅ 可测 | — |
| TC-U05 | 搜索用户 | 在搜索框输入用户名关键词，回车 | 列表过滤，仅显示匹配的用户 | ✅ 可测 | — |
| TC-U06 | 新增用户表单校验 | 新增用户时留空必填字段，点击确定 | 表单校验拦截，提示必填 | ✅ 可测 | — |

---

## 三、角色管理模块（TC-R）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-R01 | 查看角色列表 | 管理员登录后进入角色管理页 | 展示角色列表，包含 ID/角色名称/权限 | ✅ 可测 | — |
| TC-R02 | 角色增删改 | 管理员执行新增/编辑/删除角色操作 | 新增→列表出现新角色；编辑→信息更新；删除→确认后移除 | ✅ 可测 | — |

---

## 四、知识库模块（TC-K）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-K01 | 查看知识库列表 | 管理员登录后进入知识库管理页 | 展示知识库列表 | ⏳ 待开发 | 前端 B 页面为占位状态 |
| TC-K02 | 创建知识库 | 填写知识库名称/描述，提交 | 列表刷新，新知识库出现 | ⏳ 待开发 | 前端 B 页面未开发 + 后端 kb.py 未实现 |

---

## 五、文档管理模块（TC-D）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-D01 | 上传文档 | 在知识库下点击上传，选择 .md 文件 | 上传成功，文档列表出现新文档，状态为 pending | ⏳ 待开发 | 前端 B 页面 + 后端 docs.py 未实现 |
| TC-D02 | 查看文档列表 | 进入知识库，查看文档列表 | 展示文档名/类型/大小/状态/上传时间 | ⏳ 待开发 | 同上 |
| TC-D03 | 删除文档 | 点击文档行的"删除"，确认 | 文档从列表中移除 | ⏳ 待开发 | 同上 |
| TC-D04 | 文档状态自动刷新 | 上传文档后等待，观察状态变化 | pending → processing → completed，状态标签颜色自动更新 | ⏳ 待开发 | 前端 B 轮询逻辑未实现 |

---

## 六、命中测试模块（TC-H）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-H01 | 未选择文档 | 知识库已选，文档下拉留空，点击"运行测试" | 前端拦截，提示"请选择目标文档" | ⏳ 待开发 | 前端 B 页面未开发 |
| TC-H02 | 输入空问题 | 知识库+文档已选，问题输入框留空，点击"运行测试" | 前端拦截，提示"请输入测试问题" | ⏳ 待开发 | 同上 |
| TC-H03 | 检索无匹配 | 选择文档，输入与文档内容完全无关的问题 | `code: 0, total_hits: 0, hits: []`，前端展示空状态 | 🔴 阻塞 | 后端 A 接口可用但缺少鉴权（BUG-01）；前端 B 未开发 |
| TC-H04 | 三种检索模式差异化 | 同一文档+同一问题，分别切换 vector/keyword/hybrid | 三次返回结果排序不同 | 🔴 阻塞 | 同上 |
| TC-H05 | 跨知识库隔离 | 知识库 A 有 doc1，知识库 B 无文档。选择知识库 B | 文档下拉不出现 doc1 | ⏳ 待开发 | 前端 B 页面未开发 |
| **TC-H06** | **文档未处理完成（pending）** | **上传文档后立即在命中测试中选择该文档，点击运行** | **前端弹出等待提示 + 后端返回 code: 4002** | 🔴 阻塞 | 后端 A 接口缺少鉴权（BUG-01）；前端 B 未开发 |
| **TC-H07** | **文档处理失败（failed）** | **文档状态为 failed，在命中测试中选择该文档，点击运行** | **前端弹出失败提示 + 后端返回 code: 4002，data.status=failed** | 🔴 阻塞 | 同上 |
| **TC-H08** | **跨用户组越权访问** | **用户 A（组 G1）尝试检索 KB2 的文档** | **返回 code: 403** | 🔴 阻塞 | 后端 A 缺少鉴权依赖（BUG-01）；后端 B 未建 V2 表（BUG-02） |
| **TC-H09** | **未分配用户组** | **未分配用户组的用户进行命中测试** | **返回 code: 4001** | 🔴 阻塞 | 同上 |

---

## 七、智能对话模块（TC-C）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-C01 | 单轮对话 | 选择知识库，输入问题，点击发送 | 返回 AI 回答 + 引用来源 | 🔴 阻塞 | 后端 A 接口缺少鉴权（BUG-01）；前端 B 页面为占位 |
| TC-C02 | 多轮对话 | 在同一个 session_id 下连续提问 | 后续回答携带历史上下文 | 🔴 阻塞 | 同上 |
| TC-C03 | 流式对话 | 选择知识库，输入问题，观察 SSE 流式输出 | 逐字输出，结束后显示引用 | 🔴 阻塞 | 同上 |

---

## 八、降级测试模块（TC-F）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-F01 | Embedding 不可用 | 模拟 Embedding API 超时/失败 | 自动关闭向量检索，仅保留 BM25 全文检索，前端提示"向量模型暂不可用" | 🔴 阻塞 | 需要模拟 Embedding API 故障环境 |
| TC-F02 | LLM 不可用 | 模拟 LLM API 超时/失败 | 返回 5002 + 兜底文案"大模型服务暂时不可用" | 🔴 阻塞 | 需要模拟 LLM API 故障环境 |

---

## 九、权限越权专项（TC-P，V2 新增）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-P01 | 用户组内正常访问 | 用户 A（组 G1 授权 KB1）登录，查看知识库列表 | 仅显示 KB1 | 🔴 阻塞 | 后端 B 未建 V2 表（BUG-02）；后端 A 接口缺鉴权（BUG-01） |
| TC-P02 | 跨组访问知识库文档列表 | 用户 A 尝试 `GET /api/knowledge-bases/{KB2_id}/documents` | 返回 403 | 🔴 阻塞 | 同上 |
| TC-P03 | 跨组命中测试 | 用户 A 尝试 `POST /api/rag/test_retrieve` 传 KB2 的 kb_id | 返回 403 | 🔴 阻塞 | 同上 |
| TC-P04 | 跨组智能对话 | 用户 A 尝试 `POST /api/chat/send` 传 KB2 的 kb_id | 返回 403 | 🔴 阻塞 | 同上 |
| TC-P05 | 未分配用户组 | 新建用户不分配任何用户组，登录后查看知识库列表 | 列表为空，返回 4001 | 🔴 阻塞 | 同上 |
| TC-P06 | 管理员超管模式 | admin 登录，查看知识库列表 | 可查看所有知识库（不受用户组限制） | 🔴 阻塞 | 同上 |

---

## 十、文档状态准入专项（TC-S，V2 新增）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-S01 | 文档 pending 时命中测试 | 上传文档后（status=pending）立即命中测试 | 返回 4002 + 等待提示 | 🔴 阻塞 | 后端 A 缺少鉴权（BUG-01）；前端 B 未开发 |
| TC-S02 | 文档 processing 时命中测试 | 文档处理中（status=processing）命中测试 | 返回 4002 | 🔴 阻塞 | 同上 |
| TC-S03 | 文档 completed 正常检索 | 文档处理完成后（status=completed）命中测试 | 正常检索，返回命中结果 | 🔴 阻塞 | 同上 |
| TC-S04 | 文档 failed 时命中测试 | 文档处理失败后（status=failed）命中测试 | 返回 4002 + 失败提示 | 🔴 阻塞 | 同上 |
| TC-S05 | 文档下拉状态标签 | 查看命中测试页文档下拉列表 | 非 completed 文档禁用 + 状态标签显示正确（pending=灰/processing=蓝/completed=绿/failed=红） | ⏳ 待开发 | 前端 B 页面未开发 |
| TC-S06 | 知识库全部文档未就绪时对话 | KB 下所有文档都是 pending/processing/failed，发起对话 | 返回 4002 | 🔴 阻塞 | 后端 A 缺鉴权（BUG-01）；前端 B 未开发 |

---

## 十一、白标定制模块（TC-B，V2 新增）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-B01 | 未登录获取品牌配置 | `GET /api/system/branding`（无需鉴权） | 返回 200 + 品牌配置 JSON | 🔴 阻塞 | 后端 B 未开发白标接口 |
| TC-B02 | 管理员更新品牌配置 | `PUT /api/system/branding`（管理员权限） | 返回 200，配置更新 | 🔴 阻塞 | 同上 |
| TC-B03 | 普通用户更新品牌配置 | `PUT /api/system/branding`（普通用户） | 返回 403 | 🔴 阻塞 | 同上 |
| TC-B04 | 修改品牌名称 | 管理员修改 brand_name，保存 | 前端全站标题更新 | 🔴 阻塞 | 后端 B 接口 + 前端 A BrandingConfig 页面均未开发 |
| TC-B05 | 修改主题色 | 管理员修改 brand_theme_color，保存 | 前端按钮/菜单颜色更新 | 🔴 阻塞 | 同上 |
| TC-B06 | 上传新 Logo | 管理员上传新 Logo 图片，保存 | 侧边栏 + 登录页 Logo 更新 | 🔴 阻塞 | 同上 |

---

## 十二、可观测性验证（TC-O，V2 新增）

| 编号 | 场景 | 操作步骤 | 预期结果 | 状态 | 阻塞原因 |
|:--:|------|------|------|:--:|------|
| TC-O01 | 访问 /metrics 端点 | 浏览器访问 `/metrics` | 返回 Prometheus 格式指标文本 | 🔴 阻塞 | config.py 缺少 Prometheus 配置（BUG-03） |
| TC-O02 | 命中测试后指标增加 | 发起命中测试后访问 `/metrics` | `rag_retrieve_total` 计数增加 | 🔴 阻塞 | 同上 |
| TC-O03 | 对话后指标增加 | 发起对话后访问 `/metrics` | `llm_call_total` 计数增加 | 🔴 阻塞 | 同上 |
| TC-O04 | 日志 JSON 格式 | 查看 `logs/` 目录下的日志文件 | JSON 格式，包含 request_id/timestamp/level 等字段 | 🔴 阻塞 | 后端 B 未实现结构化日志 |
| TC-O05 | Langfuse Dashboard 查看 Trace | 发起对话后打开 Langfuse Dashboard | 可见完整 Trace（query → retrieval → answer → usage） | 🔴 阻塞 | config.py 缺少 Langfuse 配置（BUG-03） |

---

## 汇总

| 模块 | 用例数 | 可测 | 阻塞 | 待开发 |
|------|:--:|:--:|:--:|:--:|
| 鉴权 | 4 | 3 | 1 | 0 |
| 用户管理 | 6 | 6 | 0 | 0 |
| 角色管理 | 2 | 2 | 0 | 0 |
| 知识库 | 2 | 0 | 0 | 2 |
| 文档管理 | 4 | 0 | 0 | 4 |
| 命中测试 | 9 | 0 | 6 | 3 |
| 智能对话 | 3 | 0 | 3 | 0 |
| 降级测试 | 2 | 0 | 2 | 0 |
| 权限越权 | 6 | 0 | 6 | 0 |
| 文档状态准入 | 6 | 0 | 4 | 2 |
| 白标定制 | 6 | 0 | 6 | 0 |
| 可观测性 | 5 | 0 | 5 | 0 |
| **合计** | **55** | **11** | **33** | **11** |

> **当前可执行率：20%（11/55）**。主要阻塞项：后端 A 缺少鉴权依赖（BUG-01）、后端 B 未建 V2 表（BUG-02）、config.py 缺少 V2 配置（BUG-03）、前端 B 页面未开发。

---

*报告生成日期：2026-07-16*
# 测试环境就绪检查清单

> **版本**：V2
> **编制日期**：2026-07-16
> **编制人**：测试工程师（5号）-张宇
> **用途**：每次测试前逐项确认环境状态，确保测试结果有效

---

## 检查清单

### 一、基础设施

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | 状态 |
|:--:|------|------|------|:--:|:--:|
| 1 | Chroma 服务已启动 | `curl http://localhost:8000/api/v2/heartbeat` | 返回 200 | 项目负责人 | ☐ |
| 2 | 后端 FastAPI 已启动 | `curl http://localhost:5001/docs` | 返回 Swagger 页面 | 后端 A/B | ☐ |
| 3 | 前端 Vite 已启动 | 浏览器访问 `http://localhost:5173` | 显示登录页 | 前端 A | ☐ |
| 4 | 前端代理到后端 | `curl http://localhost:5173/api/` | 不是 404 | 前端 A | ☐ |

---

### 二、数据库

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | 状态 |
|:--:|------|------|------|:--:|:--:|
| 5 | 8 张表已建（含 V2 新增 4 张） | `python scripts/init_db.py` 或查看 SQLite | 11 张表均存在 | 后端 B | ☐ |
| 6 | 种子数据已写入 | 查询 users 表 | 至少存在 admin / editor / user 三个用户 | 后端 B | ☐ |
| 7 | system_config 默认值已初始化 | 查询 system_config 表 | 6 条品牌配置记录存在 | 后端 B | ☐ |
| 8 | 至少一个 admin 用户组已创建 | 查询 user_groups 表 | 至少 1 条记录 | 后端 B | ☐ |

---

### 三、API Key 与配置

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | 状态 |
|:--:|------|------|------|:--:|:--:|
| 9 | OpenAI API Key 可用 | 调用 Embedding API 测试 | 返回 1536 维向量 | 项目负责人 | ☐ |
| 10 | LLM 模型可用 | 调用 Chat Completions API | 正常返回文本 | 项目负责人 | ☐ |
| 11 | `.env` 中 ENV 标识不同 | 对比 6 人的 `.env` | 每人 ENV 值不同 | 项目负责人 | ☐ |
| 12 | Langfuse 配置已填入 | 检查 `.env` 中 LANGFUSE_* 三项 | 三项均非空 | 项目负责人 | ☐ |

---

### 四、测试数据

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | 状态 |
|:--:|------|------|------|:--:|:--:|
| 13 | 3 篇测试文档已准备 | 查看 `demo-materials/` | 测试文档1~3 均为 .md 格式 | 测试工程师 | ☐ |
| 14 | 10 条标准测试问题就绪 | 查看 `demo-materials/标准测试问题集.md` | 10 条问题含预期答案 | 测试工程师 | ☐ |
| 15 | 知识库已创建 | 通过 API 或管理后台创建 | 至少 1 个知识库存在 | 测试工程师 | ☐ |
| 16 | 文档已上传 | 上传 3 篇测试文档到知识库 | 3 篇文档状态均为 completed | 测试工程师 | ☐ |
| 17 | 分片已生成 | 查询 chunks 表 | 3 篇文档均有分片记录 | 测试工程师 | ☐ |

---

### 五、鉴权与权限

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | 状态 |
|:--:|------|------|------|:--:|:--:|
| 18 | 登录接口可用 | `POST /api/auth/login` | 返回 token | 后端 B | ☐ |
| 19 | 鉴权中间件生效 | 无 Token 访问受保护接口 | 返回 401 | 后端 B | ☐ |
| 20 | 权限中间件生效 | 跨组访问知识库 | 返回 403 | 后端 B | ☐ |
| 21 | 用户组体系就绪 | 调用 `GET /api/user-groups` | 返回用户组列表 | 后端 B | ☐ |

---

### 六、V2 新增功能

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | 状态 |
|:--:|------|------|------|:--:|:--:|
| 22 | 文档状态准入校验 | 对 pending 文档调用 `/test_retrieve` | 返回 4002 | 后端 A | ☐ |
| 23 | `/metrics` 端点可访问 | `curl http://localhost:5001/metrics` | 返回 Prometheus 格式文本 | 后端 B | ☐ |
| 24 | Langfuse 服务已启动 | 浏览器访问 Langfuse Host | 显示 Dashboard | 项目负责人 | ☐ |
| 25 | 白标配置接口可用 | `GET /api/system/branding` | 返回 200 + 品牌配置 | 后端 B | ☐ |
| 26 | 前端 B 页面已开发 | 浏览器访问命中测试/对话页面 | 非占位状态 | 前端 B | ☐ |

---

## 检查结果汇总

| 检查日期 | 检查人 | 总项数 | 通过 | 阻塞 | 阻塞项说明 |
|------|:--:|:--:|:--:|:--:|------|
| 2026-07-16 | 张宇 | 26 | 4 | 22 | 见下方"当前阻塞项汇总" |

---

## 当前阻塞项汇总（2026-07-16）

| # | 阻塞项 | 影响范围 | 负责修复 | 优先级 |
|:--:|------|------|:--:|:--:|
| 1 | Chroma 未启动 | 所有检索、对话功能 | 项目负责人 | P0 |
| 2 | 后端 FastAPI 未启动 | 所有 API 接口 | 后端 A/B | P0 |
| 3 | BUG-02：V2 四张表未建 | 用户组权限、白标配置 | 后端 B | P0 |
| 4 | BUG-01：后端 A 接口缺鉴权 | 命中测试、智能对话 | 后端 A | P0 |
| 5 | BUG-03：config.py 缺 Langfuse/Prometheus 配置 | 可观测性 | 后端 B | P0 |
| 6 | LLM/Embedding API Key 未确认 | 文档处理、对话 | 项目负责人 | P0 |
| 7 | 前端 B 页面未开发（占位） | 命中测试、对话、文档管理 | 前端 B | P1 |
| 8 | 无测试数据（知识库/文档/分片） | 全链路联调 | 测试工程师 | P1 |

> **当前结论**：测试环境未就绪。P0 阻塞项全部解决后，方可开始正式测试。

---

*报告生成日期：2026-07-16*
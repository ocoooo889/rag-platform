<span style="color:red"># 智能 RAG 平台 — 2 天项目排期表（V2）</span>

<span style="color:red">> 版本：V2（含五项增量任务）</span>
<span style="color:red">> 适用：6 人 × 2 天极速交付</span>
<span style="color:red">> 输出：项目负责人</span>
<span style="color:red">> **【项目负责人补充内容】**</span>

---

## Day1

### 上午（09:00 - 13:00）

| 时段 | 项目负责人 | 后端 A（RAG） | 后端 B（CRUD） | 前端 A | 前端 B | 测试 |
|:--:|------|------|------|------|------|------|
| **09:00-09:30** | ① 全员晨会：确认分工、环境准备、注意事项<br>② 分发 API Key，确认可用 | ① 搭建项目骨架<br>② 配置 Python 虚拟环境 | ① 搭建项目骨架<br>② 配置 Python 虚拟环境 | ① 初始化 Vue3 + Vite 项目<br>② 安装依赖 | ① 初始化 Vue3 + Vite 项目<br>② 安装依赖 | ① 阅读 API 契约<br>② 熟悉项目结构 |
| **09:30-10:30** | ① 输出 `2天项目排期表.md`<br>② 输出 `风险与预案清单.md` | ① 数据库表模型定义<br>② **Langfuse SDK 初始化** | ① 数据库表模型定义（含用户组表）<br>② **结构化日志中间件** | ① 全局布局 + 路由配置<br>② **白标配置拉取机制** | ① 全局布局 + 路由配置<br>② Mock 数据对接 | ① 编写全模块测试用例框架<br>② **权限越权用例编写** |
| **10:30-11:30** | ① 输出 `api_contract.md`<br>② 输出 `db_schema.md` | ① Embedding 工具类开发<br>② 切片工具类开发 | ① 登录鉴权模块（JWT）<br>② 用户/角色 CRUD | ① 系统概览页开发<br>② 权限控制实现 | ① 知识库管理页开发<br>② Mock 接口对接 | ① 接口文档校验<br>② 测试环境就绪检查 |
| **11:30-12:30** | ① 确认全员 .env 配置正确<br>② 输出 `monitoring_guide.md` | ① 向量检索接口开发<br>② **Langfuse Trace 包裹检索链路** | ① **用户组 CRUD**<br>② **权限中间件** | ① 角色管理页开发<br>② **用户组管理页面** | ① 文档管理页开发<br>② 列表展示 | ① 单模块冒烟测试准备<br>② **文档状态准入用例编写** |
| **12:30-13:00** | **CP1 检查点**：全员本地启动成功、数据库建表完成、Mock 接口可调通 | - | - | - | - | - |

### 下午（13:00 - 19:00）

| 时段 | 项目负责人 | 后端 A（RAG） | 后端 B（CRUD） | 前端 A | 前端 B | 测试 |
|:--:|------|------|------|------|------|------|
| **13:00-14:00** | ① 盯后端 A/B 主动脉联调<br>② 解决环境问题 | ① 文档上传→切片→向量化链路开发 | ① 文档上传接口 + 文件存储<br>② 知识库 CRUD | ① 用户管理页开发<br>② 表单校验 | ① 文档管理页完善<br>② 文件上传组件 | ① 单模块冒烟测试<br>② 命中测试专项用例编写 |
| **14:00-15:00** | ① 协调前后端接口联调<br>② 输出 `交付验收标准.md` | ① BM25 全文检索开发<br>② 混合检索融合公式实现 | ① 大模型管理 CRUD<br>② **白标配置接口** | ① 大模型管理页开发<br>② **品牌配置页面** | ① **命中率测试页完整 UI**<br>② **文档状态禁用逻辑** | ① 已开发接口回归测试<br>② **文档状态准入测试** |
| **15:00-16:00** | ① 联调问题汇总<br>② 输出 `演示操作流程.md` | ① 命中测试接口开发<br>② **文档状态准入校验** | ① **Prometheus /metrics 端点**<br>② 分页接口 | ① 页面弹窗 + 筛选分页<br>② **白标渲染联调** | ① 智能对话页面骨架<br>② 聊天窗口布局 | ① 权限越权冒烟测试<br>② 跨组访问阻断验证 |
| **16:00-17:00** | ① 盯进度，解决阻塞问题<br>② 准备演示素材 | ① RAG 对话链路开发<br>② LLM 调用封装 | ① **所有 KB/文档接口加用户组过滤**<br>② 权限拦截完善 | ① 样式统一<br>② 剩余 bug 修复 | ① 三种检索切换交互<br>② 流式渲染准备 | ① 全流程串联测试准备<br>② **权限隔离全链路测试** |
| **17:00-18:00** | ① Bug 汇总 + 分配优先级<br>② 代码 Review | ① bug 修复 + 检索速度优化<br>② **Langfuse 对话 Trace** | ① bug 修复 + **监控指标校验**<br>② 数据导出接口 | ① bug 修复<br>② **白标切换验证** | ① bug 修复<br>② 联调问题解决 | ① 回归测试<br>② 兼容性校验 |
| **18:00-19:00** | **CP2 检查点**（19:00）：全员合入 dev，验证主动脉贯通 | - | - | - | - | - |

### 晚间（19:00 - 21:00）

| 时段 | 项目负责人 | 后端 A（RAG） | 后端 B（CRUD） | 前端 A | 前端 B | 测试 |
|:--:|------|------|------|------|------|------|
| **19:00-20:00** | ① 合入代码审查<br>② 构建验证 | ① 混合检索 + 命中测试接口完成<br>② **文档状态准入校验生效** | ① 权限拦截 + 下拉筛选<br>② 所有接口加用户组过滤 | ① 大模型管理页面完成<br>② **品牌配置页面完成** | ① 智能对话页面骨架完成<br>② 基础聊天功能 | ① 已开发接口回归测试<br>② **权限越权冒烟测试** |
| **20:00-21:00** | ① 日报汇总<br>② 次日计划 | ① 代码清理<br>② 接口文档完善 | ① 代码清理<br>② 接口文档完善 | ① 代码清理<br>② 页面优化 | ① 代码清理<br>② 页面优化 | ① 测试报告整理<br>② 问题汇总 |

---

## Day2

### 上午（09:00 - 12:00）

| 时段 | 项目负责人 | 后端 A（RAG） | 后端 B（CRUD） | 前端 A | 前端 B | 测试 |
|:--:|------|------|------|------|------|------|
| **09:00-10:00** | ① 组织前后端联调<br>② 解决遗留问题 | ① RAG 对话链路完善<br>② LLM 流式对接 | ① 与后端 A 数据互通<br>② 数据导出功能 | ① 页面弹窗 + 筛选分页完善<br>② **白标渲染联调** | ① 命中测试页对接真实接口<br>② 智能对话聊天窗口 | ① 全流程串联测试<br>② **权限隔离全链路测试** |
| **10:00-11:00** | ① 联调进度跟踪<br>② 演示脚本演练 | ① 熔断降级机制<br>② **Langfuse Token 用量上报** | ① **Prometheus 自定义指标完善**<br>② 检索延迟/LLM 延迟/命中率指标 | ① 表单校验完善<br>② 异常提示优化 | ① 三种检索切换交互完善<br>② **文档状态禁用联调** | ① 回归测试<br>② 命中测试专项 |
| **11:00-12:00** | **CP3 检查点**（12:00）：全链路贯通、用户组权限隔离生效、白标配置可切换 | - | - | - | - | - |

### 下午（13:00 - 18:00）

| 时段 | 项目负责人 | 后端 A（RAG） | 后端 B（CRUD） | 前端 A | 前端 B | 测试 |
|:--:|------|------|------|------|------|------|
| **13:00-14:00** | ① 汇总 bug + 分配优先级<br>② 输出交付文档 | ① bug 修复 + 检索速度优化 | ① bug 修复 + **监控指标校验** | ① 样式统一<br>② 剩余 bug 修复 | ① 三种检索切换交互完善<br>② 流式渲染 | ① 回归测试<br>② 兼容性校验 |
| **14:00-15:00** | ① 演示准备<br>② 交付文档完善 | ① 性能优化<br>② 接口文档最终确认 | ① 性能优化<br>② **监控指标最终校验** | ① **白标切换验证**<br>② 页面优化 | ① 智能对话流式渲染<br>② 联调问题解决 | ① 测试报告编写<br>② **可观测性验证** |
| **15:00-16:00** | ① 演示彩排<br>② 交付验收标准确认 | ① 代码审查<br>② 最终验证 | ① 代码审查<br>② 最终验证 | ① 代码审查<br>② 最终验证 | ① 代码审查<br>② 最终验证 | ① 测试报告最终确认<br>② 验收用例执行 |
| **16:00-17:00** | **最终交付检查**：所有功能验证通过、文档齐全、代码可构建 | - | - | - | - | - |
| **17:00-18:00** | ① 项目总结<br>② 交付汇报 | - | - | - | - | - |

---

## 检查点纪律

| 检查点 | 时间 | 硬性通过标准 |
|:--:|------|------|
| **CP1** | Day1 13:00 | 全员本地启动成功、数据库建表完成（含用户组表 + 系统配置表）、Mock 接口可调通 |
| **CP2** | Day1 19:00 | 文档上传→切片→向量化→检索 主动脉贯通；**文档状态准入校验生效** |
| **CP3** | Day2 12:00 | 全链路贯通（创建知识库→上传文档→**等待处理完成**→命中测试→智能对话）；**用户组权限隔离生效**；**白标配置可切换** |

---

## V2 新增任务标注

| 角色 | V2 新增任务 | 完成时限 |
|------|------|:--:|
| 后端 A | Langfuse SDK 初始化、Langfuse Trace 包裹检索链路、Langfuse 对话 Trace + Token 用量上报 | Day1 下午 |
| 后端 B | 用户组表结构 + 结构化日志中间件、用户组 CRUD + 权限中间件、白标配置接口、Prometheus /metrics 端点、所有 KB/文档接口加用户组过滤、Prometheus 自定义指标完善 | Day1 全天 |
| 前端 A | 白标配置拉取机制、用户组管理页面、品牌配置页面、白标渲染联调、白标切换验证 | Day1 全天 |
| 前端 B | 文档状态禁用逻辑、文档状态禁用联调 | Day1 下午 |
| 测试 | 权限越权用例编写、文档状态准入用例编写、权限越权冒烟测试、权限隔离全链路测试、可观测性验证 | Day1 全天 |

---

*排期表版本：V2*<br>
*输出日期：2026-07-17*
# 2 天项目排期表 · Day2 工作表（可验收版）

> **版本**：Day2 开工特刊（基于 `RAG项目完整管控方案-V2.md` + Day1 测试结论 + 后端 B 最新复测）  
> **目标日期**：Day2（以 CP3 12:00 与当日下午演示验收为硬节点）  
> **编制说明**：本表替换原空白《2天项目排期表》，聚焦 **Day2 全员职责与可验收路径**；Day1 任务仅作「遗留门禁」引用，不再展开。

---

## 〇、Day2 验收定义（先对齐再开工）

### CP3 硬性通过标准（V2 · 12:00）

| # | 硬性项 | 判定标准 | 主责 |
|:--:|------|------|:--:|
| 1 | 全链路贯通 | 创建知识库 → 上传文档 → **等待 completed** → 命中测试 → 智能对话 | 全员 |
| 2 | 用户组权限隔离 | 非 admin 仅见授权 KB；跨组访问 403；未分配组 4001 | 后端 B + A + 测试 |
| 3 | 白标可切换 | GET 公开可用；admin PUT 后名称/Logo（至少一项）生效 | 后端 B + 前端 A |

### P0 必保（不可砍，V2 第十四章）

命中率测试（三模式）· 知识库/文档 CRUD · 智能对话 · 文档状态准入（4002）· 字段名 `query` · 结构化日志

### 有条件通过（时间不够时的底线）

| 项 | 可降级为 |
|------|----------|
| 用户组 | 列表隔离 + 文档 403 必须；rag/chat 跨组 403 可晚 2h 补 |
| Prometheus | `/metrics` HTTP 自动指标即可；业务 Counter 下午补 |
| Langfuse | 仅 chat Trace；无服务则记「环境未起」 |
| 白标 | 名称 + Logo；主题色可二期 |
| 前端 B | 若 UI 未齐，Swagger/脚本可证后端，UI 记遗留但须有最小命中+对话页 |

---

## 一、Day2 开局门禁（09:00 前必须闭环）

> 不过门禁，禁止进入「功能打磨」；全员阻塞在入口联调。

| 门禁 | 动作 | 负责 | 完成标志 |
|:--:|------|:--:|------|
| G1 | `main.py` 注册后端 B 全部路由 + 异常码统一 + 种子数据 | **后端 B** | OpenAPI 含 `/api/auth/login`、`/api/knowledge-bases`、`/api/user-groups`、`/api/system/branding` |
| G2 | Instrumentator → `/metrics` + 日志中间件挂到 main | **后端 B** | 浏览器打开 `/metrics` 有 Prometheus 文本 |
| G3 | rag/chat 与 B **同一 SQLite**；A 侧加 `get_current_user` + `require_kb_access` | **后端 A + B** | 用非授权 KB 调 chat/rag 返回 403，不再报「无表」 |
| G4 | 全员 `.env` / API Key / Chroma `:8000` 可用 | **项目负责人** 检查 | `health` 正常；上传文档能到 `completed`（或明确降级说明） |
| G5 | 前端指向真实后端（去掉仅 Mock 演示依赖） | **前端 A/B** | 登录走真实 `/api/auth/login` |

**门禁负责人签字时间：09:30**（延误则负责人升级，砍非 P0）。

---

## 二、Day2 全员时段总表

| 时段 | 项目负责人 | 后端 A（RAG） | 后端 B（CRUD） | 前端 A | 前端 B | 测试 |
|------|------|------|------|------|------|------|
| **08:30–09:30 门禁** | 卡 G1–G5；统一分支合入 `dev`；确认 Key/Chroma | 配合同库 + **给 rag/chat 加鉴权依赖（BUG-01）** | **完成 main 挂载 B + metrics/logger（BUG-D1-03/04）**；确认 init/种子 | 对接真实登录；白标启动拉取 | 确认 API base；清占位依赖 | 用 OpenAPI/冒烟验证门禁；更新用例「可测」状态 |
| **09:30–11:00 冲 CP3 功能** | 盯主动脉：上传→completed→检索→对话 | 对话流式稳定；4002 回归；Langfuse chat Trace；熔断 5002 | 与 A 数据互通；dashboard/stats；用户组过滤回归；上传权限 | 用户组管理页；品牌配置页；筛选/弹窗/校验；白标渲染 | **命中测试对接真实接口**；**对话窗 + SSE**；文档状态下拉禁用 | 权限 TC-P01/P02/P05/P06；白标 TC-B01~B03；上传/列表冒烟 |
| **11:00–12:00 CP3 演练** | **主持 CP3**：按脚本走一遍；记缺口 | 保障检索/对话现场可用 | 保障鉴权/KB/上传现场可用 | 保障管理端+白标切换 | 保障命中+对话 UI 可点 | **全流程串联 + 权限隔离全链路**；出 CP3 初判 |
| **12:00 CP3 检查点** | 宣布：通过 / 有条件通过 / 不通过 | 认领 P0 修复 | 认领 P0 修复 | 认领 P0 修复 | 认领 P0 修复 | 固化失败用例编号 |
| **13:30–16:00 修 Bug + 验收打磨** | 汇总 Bug 优先级；演示脚本定稿 | Bug 修复；检索优化；边界 top_n/SSE | Bug 修复；监控指标校验；导出（可选） | 样式统一；白标切换验证；V2 错误码提示 | 三模式切换；流式渲染；联调收尾 | 回归 + 兼容性 + **可观测性 TC-O**；出测试报告 |
| **16:00–17:30 演示验收** | 按演示脚本验收；收交付清单 | 待命修现场问题 | 待命修现场问题 | 待命修现场问题 | 待命修现场问题 | 陪跑演示清单；记录遗留 |

---

## 三、分角色 Day2 任务卡（可勾选）

### 3.1 项目负责人

| 优先级 | 任务 | 完成标准 |
|:--:|------|------|
| P0 | 09:30 前门禁 G1–G5 闭环 | 全员同一 `dev` 可启动 |
| P0 | 12:00 主持 CP3 | 三项硬性标准有书面结论 |
| P0 | 演示脚本 + 验收账号（admin / 组用户 / 无组用户） | 演示可复现 |
| P1 | 下午 Bug 分派与截止时间 | P0 当日清、P1 有责任人 |
| P1 | 交付文档：部署说明 / 已知问题 / 二期清单 | 与 V2 第十四章降级策略一致 |

### 3.2 后端 A（RAG）

| 优先级 | 任务 | 对应遗留 |
|:--:|------|------|
| P0 | rag.py / chat.py 增加鉴权 + `require_kb_access` | BUG-01 |
| P0 | 与 B 同库；合入后不再 `no such table` | BUG-B02 |
| P0 | 保障 keyword/vector/hybrid + 对话（有 Key）可用 | CP3 全链路 |
| P0 | 文档非 completed → 4002 保持有效 | TC-S / 准入 |
| P1 | Langfuse 对话 Trace + Token | V2 可降级 |
| P2 | top_n 边界、SSE 断连 | BUG-08/09 |

### 3.3 后端 B（CRUD）

| 优先级 | 任务 | 对应遗留 |
|:--:|------|------|
| P0 | **main 注册全部 B 路由** + 统一异常 `{code,msg,data}` | BUG-D1-03 |
| P0 | Prometheus + 日志中间件挂 main | BUG-D1-04 |
| P0 | init/种子含 V2 四表 + admin | BUG-02/07 |
| P0 | 用户组过滤 & 文档权限保持有效 | TC-P |
| P1 | `GET /api/dashboard/stats` | BUG-B03 |
| P1 | 自定义指标（检索/LLM 延迟）埋点校验 | TC-O02/O03 |
| P2 | 数据导出（可砍） | V2 P2 |

### 3.4 前端 A（管理后台）

| 优先级 | 任务 | 对应遗留 |
|:--:|------|------|
| P0 | 真实登录联调；管理页走真实 API | 门禁 G5 |
| P0 | **UserGroupManage** 可用（成员 + KB 授权） | BUG-D1-05 |
| P0 | **BrandingConfig** + 启动拉白标并渲染 | BUG-D1-05 / CP3 |
| P1 | `request.js` 处理 4001/4002/4003/403 | BUG-F03 |
| P1 | 筛选分页弹窗校验 | V2 Day2 |
| P2 | `type="text"` → link；登录已登跳转 | BUG-F02/F04 |

### 3.5 前端 B（RAG 页面）

| 优先级 | 任务 | 对应遗留 |
|:--:|------|------|
| P0 | **HitTest** 对接 `/api/rag/test_retrieve`（字段 `query`） | BUG-D1-02 |
| P0 | **ChatDialog** 对接 send/stream | BUG-D1-02 |
| P0 | 文档状态下拉：非 completed 禁用 + 4002 提示 | TC-S05 |
| P1 | KbManage + DocManage 真实 CRUD/上传 | CP3 上传入口 |
| P1 | 三种检索切换 + 流式渲染 | V2 Day2 下午 |

### 3.6 测试工程师

| 优先级 | 任务 | 产出 |
|:--:|------|------|
| P0 | 门禁后冒烟（登录/KB/用户组/白标/上传） | 冒烟记录 |
| P0 | 权限专项 TC-P01/P02/P05/P06（入口合入后正式环境） | 证据 |
| P0 | CP3 全流程串联（手册演示 9 步） | CP3 结论 |
| P1 | 文档状态 TC-S01~S04、S06 | 专项结果 |
| P1 | 可观测 TC-O01~O04（O05 视 Langfuse） | 报告章节 |
| P0 | Day2 测试报告 + 遗留 Bug 清单 | 交付物 |

---

## 四、CP3 演示脚本（12:00 / 下午验收共用）

| 步 | 操作 | 期望 | 失败则 |
|:--:|------|------|------|
| 1 | admin 登录 | 进管理端 | 查 auth / 前端路由 |
| 2 | 修改品牌名称（或 Logo）并保存 | 刷新后可见 | 查 branding PUT/前端 store |
| 3 | 创建用户组 G1 → 添加用户 A → 授权 KB1 | 接口成功 | 查 user-groups |
| 4 | 创建 KB1 → 上传 md → 至 **completed** | 状态变绿 | 查 Chroma/Key/ingest |
| 5 | 用户 A 登录 → 知识库列表仅 KB1 | TC-P01 | 查 kb 过滤 |
| 6 | 命中测试三模式各 1 次 | 有 hits 或可解释空结果 | 查 rag / 前端 query |
| 7 | 智能对话 1 问 | 有 answer + 引用或 5002 降级说明 | 查 LLM Key |
| 8 | 再传新文档 → 立刻命中 | **4002** | 查准入 |
| 9 | 用户 A 访 KB2 文档/检索 | **403** | 查 permission；A 未加鉴权则仅文档列表 403 算部分分 |
| 10 | 打开 `/metrics` | Prometheus 文本 | 查 Instrumentator |

---

## 五、合入与协作纪律（Day2）

| 规则 | 要求 |
|------|------|
| 分支 | 功能分支 → 及时 PR/合入 `dev`；**09:30 前至少一版可跑的 main** |
| 提交信息 | `feat/fix/docs` + 模块名；禁止提交空文件占位冒充完成 |
| 接口契约 | 检索/对话输入统一 **`query`**；响应 `{code,msg,data}` |
| 联调频道 | 阻塞 >30 分钟 @负责人；P0 不过夜 |
| 环境 | 每人 `ENV` 隔离；演示机单独一套 Key + DB + collection |

---

## 六、Day2 交付物清单（验收收口）

| 角色 | 交付物 |
|------|------|
| 负责人 | 演示脚本、部署说明、已知问题、二期清单 |
| 后端 A | 可演示的检索+对话；鉴权合入；降级行为说明 |
| 后端 B | 正式入口可用的 CRUD+用户组+白标+`/metrics` |
| 前端 A | 管理端 + 用户组 + 白标切换 |
| 前端 B | 命中测试 + 智能对话（+ 知识库/文档尽量齐） |
| 测试 | Day2 测试报告、CP3 结论、遗留 Bug 表 |

---

## 七、与 Day1 排期的关系

| Day1（已发生） | Day2（本表） |
|------|------|
| CP1/CP2 未完全通过；B 曾空文件、前端 B 占位 | **先补门禁，再冲 CP3，下午验收** |
| 用例可执行率约 20% | 目标门禁后 ≥ 45%，CP3 后出具正式报告 |
| 详见 `docs/Day1：测试报告/*`、`docs/metting/Day2_测试工程师开工报告及建议.md` | 本表为 **全员执行版排期** |

---

**一句话**：Day2 上午先让 **main 挂上 B + A 鉴权同库**，中午按脚本过 **CP3 三硬性项**，下午只修 P0/P1 并完成演示——其余按 V2 降级策略进二期，不挡验收。

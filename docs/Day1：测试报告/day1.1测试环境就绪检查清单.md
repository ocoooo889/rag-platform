# 测试环境就绪检查清单

> **版本**：V2
> **编制日期**：2026-07-16
> **编制人**：测试工程师（5号）-张宇
> **用途**：每次测试前逐项确认环境状态，确保测试结果有效

---

## 检查清单

### 一、基础设施

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | Day1 状态 |
|:--:|------|------|------|:--:|:--:|
| 1 | Chroma 服务已启动 | `curl http://localhost:8000/api/v1/heartbeat` | 返回 200 | 项目负责人 | ☐ |
| 2 | 后端 FastAPI 已启动 | `curl http://localhost:5001/docs` | 返回 Swagger 页面 | 后端 A/B | ☐ |
| 3 | 前端 Vite 已启动 | 浏览器访问 `http://localhost:5173` | 显示登录页 | 前端 A | ☐ |
| 4 | 前端代理到后端 | `curl http://localhost:5173/api/` | 不是 404 | 前端 A | ☐ |

### 二、数据库

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | Day1 状态 |
|:--:|------|------|------|:--:|:--:|
| 5 | 11 张表已建（含 V2 新增 4 张） | `python scripts/init_db.py` | 11 张表均存在 | 后端 B | ❌ 仅 7 张 |
| 6 | 种子数据已写入 | 查询 users 表 | 至少存在 admin/editor/user 三个用户 | 后端 B | ❌ 仅 admin |
| 7 | system_config 默认值已初始化 | 查询 system_config 表 | 6 条品牌配置记录存在 | 后端 B | ❌ 表未建 |
| 8 | 至少一个 admin 用户组已创建 | 查询 user_groups 表 | 至少 1 条记录 | 后端 B | ❌ 表未建 |

### 三、API Key 与配置

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | Day1 状态 |
|:--:|------|------|------|:--:|:--:|
| 9 | OpenAI API Key 可用 | 调用 Embedding API 测试 | 返回 1536 维向量 | 项目负责人 | ☐ |
| 10 | LLM 模型可用 | 调用 Chat Completions API | 正常返回文本 | 项目负责人 | ☐ |
| 11 | `.env` 中 ENV 标识不同 | 对比 6 人的 `.env` | 每人 ENV 值不同 | 项目负责人 | ☐ |
| 12 | Langfuse 配置已填入 | 检查 `.env` 中 LANGFUSE_* 三项 | 三项均非空 | 项目负责人 | ☐ |

### 四、测试数据

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | Day1 状态 |
|:--:|------|------|------|:--:|:--:|
| 13 | 3 篇测试文档已准备 | 查看 `demo-materials/` | 测试文档1~3 均为 .md 格式 | 测试工程师 | ✅ |
| 14 | 10 条标准测试问题就绪 | 查看 `demo-materials/标准测试问题集.md` | 10 条问题含预期答案 | 测试工程师 | ✅ |
| 15 | 知识库已创建 | 通过 API 或管理后台创建 | 至少 1 个知识库存在 | 测试工程师 | ❌ 后端 B 未实现 |
| 16 | 文档已上传 | 上传 3 篇测试文档到知识库 | 3 篇文档状态均为 completed | 测试工程师 | ❌ 后端 B 未实现 |
| 17 | 分片已生成 | 查询 chunks 表 | 3 篇文档均有分片记录 | 测试工程师 | ❌ 无文档可上传 |

### 五、鉴权与权限

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | Day1 状态 |
|:--:|------|------|------|:--:|:--:|
| 18 | 登录接口可用 | `POST /api/auth/login` | 返回 token | 后端 B | ❌ 未实现 |
| 19 | 鉴权中间件生效 | 无 Token 访问受保护接口 | 返回 401 | 后端 B | ❌ 未实现 |
| 20 | 权限中间件生效 | 跨组访问知识库 | 返回 403 | 后端 B | ❌ 未实现 |
| 21 | 用户组体系就绪 | 调用 `GET /api/user-groups` | 返回用户组列表 | 后端 B | ❌ 未实现 |

### 六、V2 新增功能

| # | 检查项 | 验证命令/方法 | 就绪标准 | 负责人 | Day1 状态 |
|:--:|------|------|------|:--:|:--:|
| 22 | 文档状态准入校验 | 对 pending 文档调用 `/test_retrieve` | 返回 4002 | 后端 A | ✅ 已实现 |
| 23 | `/metrics` 端点可访问 | `curl http://localhost:5001/metrics` | 返回 Prometheus 格式文本 | 后端 B | ❌ 未实现 |
| 24 | Langfuse 服务已启动 | 浏览器访问 Langfuse Host | 显示 Dashboard | 项目负责人 | ☐ |
| 25 | 白标配置接口可用 | `GET /api/system/branding` | 返回 200 + 品牌配置 | 后端 B | ❌ 未实现 |
| 26 | 前端 B 页面已开发 | 浏览器访问命中测试/对话页面 | 非占位状态 | 前端 B | ❌ 占位 |

---

## Day1 检查结果汇总

| 检查日期 | 检查人 | 总项数 | 通过 | 未通过 | 通过率 |
|------|:--:|:--:|:--:|:--:|:--:|
| 2026-07-16 | 张宇 | 26 | 2 | 24 | 8% |

---

## Day1 阻塞项汇总

| # | 阻塞项 | 影响范围 | 负责修复 | 优先级 |
|:--:|------|------|:--:|:--:|
| 1 | 后端 B 全部 CRUD 接口未实现（8 个空文件） | 登录、用户、角色、知识库、文档、模型、用户组、白标 | 后端 B | P0 |
| 2 | 前端 B 全部 RAG 页面未实现（4 个占位/空） | 知识库管理、文档管理、命中测试、智能对话 | 前端 B | P0 |
| 3 | V2 四张表未建 + 种子数据缺失 | 用户组权限、白标配置 | 后端 B | P0 |
| 4 | Chroma 未启动 | 所有检索、对话功能 | 项目负责人 | P0 |
| 5 | 后端 FastAPI 未启动 | 所有 API 接口 | 后端 A/B | P0 |
| 6 | LLM/Embedding API Key 未确认 | 文档处理、对话 | 项目负责人 | P0 |

> **Day1 结论**：测试环境就绪率仅 8%。后端 A 的 RAG 引擎和前端 A 的管理后台已就绪，但后端 B 的 CRUD 业务层和前端 B 的 RAG 页面层完全缺失，无法进行全链路集成测试。

---

*报告生成日期：2026-07-16*